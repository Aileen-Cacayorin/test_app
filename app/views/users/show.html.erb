<h2>This is the user page</h2>

<% if @address_books.any? %>
  <ul>
    <% @address_books.each do |address_book| %>
      <li> <%= link_to address_book.name, user_address_book_path(@user, address_book) %> </li>
    <% end %>
  </ul>
<% end  %>

<%= link_to 'Add Address Book', new_user_address_book_path(@user) %>
